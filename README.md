# 🏋️‍♂️ Fitness & Health Telegram Bot

Telegram-бот для отслеживания потребления воды, калорий и тренировок.

## 📋 Функционал

### Основные функции:
- 👤 **Настройка профиля** — вес, рост, возраст, пол, уровень активности, город
- 💧 **Трекинг воды** — логирование и отслеживание нормы с учётом погоды
- 🍎 **Трекинг еды** — поиск калорийности продуктов (100+ продуктов в базе)
- 🏃 **Трекинг тренировок** — расчёт сожжённых калорий для 40+ видов активности
- 📊 **Прогресс** — детальная статистика за день
- 📈 **Графики** — визуализация прогресса за неделю
- 💡 **Рекомендации** — советы по питанию и тренировкам

### Команды бота:
| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/help` | Помощь по командам |
| `/set_profile` | Настроить профиль |
| `/my_profile` | Посмотреть профиль |
| `/log_water [мл]` | Записать воду |
| `/log_food [продукт]` | Записать еду |
| `/log_workout [тип] [мин]` | Записать тренировку |
| `/check_progress` | Проверить прогресс |
| `/show_charts` | Графики за неделю |
| `/recommendations` | Советы |

## 🚀 Запуск

### Вариант 1: Локальный запуск (без Docker)

1. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Создайте файл `.env`:**
```env
BOT_TOKEN=ваш_токен_бота
WEATHER_API_KEY=ваш_api_ключ_openweathermap
DATABASE_PATH=bot_database.db
```

4. **Запустите бота:**
```bash
python bot.py
```

### Вариант 2: Запуск через Docker

1. **Создайте файл `.env` (как описано выше)**

2. **Соберите и запустите контейнер:**
```bash
docker-compose up -d --build
```

3. **Просмотр логов:**
```bash
docker-compose logs -f
```

4. **Остановка:**
```bash
docker-compose down
```

### Вариант 3: Деплой на Render.com

1. **Загрузите код на GitHub**

2. **Создайте новый Web Service на Render.com:**
   - Выберите ваш репозиторий
   - Environment: Docker
   - Instance Type: Free

3. **Добавьте Environment Variables:**
   - `BOT_TOKEN` — токен вашего бота
   - `WEATHER_API_KEY` — API ключ OpenWeatherMap

4. **Нажмите "Create Web Service"**

## 📁 Структура проекта

```
TelegramBot/
├── bot.py              # Главный файл бота
├── config.py           # Конфигурация
├── database.py         # Работа с SQLite
├── handlers/           # Обработчики команд
│   ├── __init__.py
│   ├── common.py       # /start, /help
│   ├── profile.py      # /set_profile, /my_profile
│   ├── water.py        # /log_water
│   ├── food.py         # /log_food
│   ├── workout.py      # /log_workout
│   └── progress.py     # /check_progress, /show_charts
├── utils/              # Утилиты
│   ├── __init__.py
│   ├── weather.py      # API погоды
│   ├── food_api.py     # Поиск калорийности
│   ├── calculations.py # Расчёты норм
│   └── charts.py       # Генерация графиков
├── requirements.txt    # Зависимости
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose
├── .env.example        # Пример переменных
└── README.md           # Документация
```

## 🔧 Технологии

- **Python 3.10+**
- **aiogram 3.x** — асинхронный фреймворк для работы Telegram бота
- **SQLite** — база данных
- **aiohttp** — асинхронные HTTP запросы
- **matplotlib** — графики
- **OpenWeatherMap API** — погода
- **OpenFoodFacts API** — калорийность продуктов

## 📊 Формулы расчёта

### Норма воды:
```
Базовая норма = Вес × 30 мл
+ 500 мл за каждые 30 минут активности
+ 500-1000 мл при температуре > 25°C
```

### Норма калорий (Миффлина-Сан Жеора):
```
Мужчины: BMR = 10 × Вес + 6.25 × Рост - 5 × Возраст + 5
Женщины: BMR = 10 × Вес + 6.25 × Рост - 5 × Возраст - 161

Дневная норма = BMR × Коэффициент активности (1.2 - 1.9)
```

## 📝 Получение API ключей

### Telegram Bot Token:
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

### OpenWeatherMap API:
1. Зарегистрируйтесь на https://openweathermap.org
2. Перейдите в API Keys
3. Создайте новый ключ (бесплатно)

## 👨‍💻 Автор

Иванов Артём Юрьевич
Создано в рамках курса "Прикладной Python" в ВШЭ

## 📄 Лицензия

MIT License


